<!DOCTYPE html>
<html lang="id">
<head>
  <%- include('partials/head') %>
  <title>Beranda - Toko Sembako Ariesta</title>
</head>
<body class="font-body text-brand-dark min-h-screen flex flex-col">
  <%- include('partials/navbar') %>

  <main class="max-w-6xl mx-auto px-6 flex-1">
    <section class="hero-title text-center py-16">
      <h1 class="font-heading text-4xl font-bold text-brand-dark mb-3">
        Selamat Datang di Toko Sembako Ariesta
      </h1>
      <p class="text-lg text-brand-dark/70">Belanja kebutuhan pokok jadi lebih mudah</p>
    </section>

    <section class="pb-16">
      <h2 class="font-heading text-2xl font-semibold text-brand-dark mb-6 text-center">
        Produk Pilihan
      </h2>
      <div class="grid grid-cols-1 md:grid-cols-3 gap-6">
        <% preview.forEach(p => { %>
          <article class="glass-card rounded-2xl p-6 hover:shadow-xl transition">
            <h3 class="font-heading font-semibold text-lg text-brand-dark mb-2"><%= p.name %></h3>
            <p class="text-brand-gold font-semibold">Rp <%= p.price.toLocaleString('id-ID') %></p>
          </article>
        <% }) %>
      </div>
    </section>
  </main>

  <%- include('partials/footer') %>
  <script src="/js/main.js"></script>
</body>
</html>